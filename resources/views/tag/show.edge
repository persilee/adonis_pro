@layout('layouts.main')

@section('content')
  @set('pageTitle', tag.title)
  <h1 class="my-5 font-weight-light text-center">{{ tag.title }}</h1>
  @!component('components.post.list', {
    items: data
  })
  @!component('components.pagination', {
    total,
    perPage,
    page,
    lastPage
  })
@endsection
