@layout('layouts.main')
@section('content')
  @set('pageTitle', 'Posts')
  <div class="post-content">
    @!component('components.post.list', {
      items: data
    })
  </div>
  @!component('components.pagination', {
    total,
    perPage,
    page,
    lastPage,
  })
@endsection
