@layout('layouts.main')
@section('content')
  @set('pageTitle', 'Posts')
  <div class="post-content">
    @!component('components.post.list', {
      items: data,
      userId
    })
  </div>
  @if(total > perPage)
    @!component('components.pagination', {
      total,
      perPage,
      page,
      lastPage,
    })
  @endif
@endsection
