<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  {{ style('css/style') }}
  {{ script('js/main') }}
  <title>{{ pageTitle || 'lishaoy' }}</title>
</head>
<body style="background-image: url({{ assetsUrl('splash.png') }})">
  <div><small>URL:{{ request.url() }}</small></div> <br />
  <div><small>OriginalURL:{{ request.originalUrl() }}</small></div> <br />
  <div><small>Method:{{ request.method() }}</small></div> <br />
  <div><small>Protocol:{{ request.protocol() }}</small></div> <br />
  <div class="container">
    <div class="header">
      @section('header')
      <small>__headerSection__</small>
      @endsection
    </div>
    <div class="content">
      @section('content')
        <p>here is no content yet.</p>
      @endsection
    </div>
    @include('partials.footer')
  </div>
</body>
</html>
